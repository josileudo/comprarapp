import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    gap: 7,
  },
  description: {
    fontSize: 12,
  },
  delete: {
    marginLeft: "auto",
  },
});
